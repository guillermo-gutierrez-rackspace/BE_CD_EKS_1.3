apiVersion: v1
kind: Service
metadata:
  namespace: NAMESPACE_PLACEHOLDER
  name: SERVICENAME_PLACEHOLDER-bknd-svc-8080
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: NodePort
  selector:
    app.kubernetes.io/name: SERVICENAME_PLACEHOLDER-backend-GREENCOLOR_PLACEHOLDER
