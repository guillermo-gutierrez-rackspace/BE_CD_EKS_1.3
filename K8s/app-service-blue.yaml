apiVersion: v1
kind: Service
metadata:
  namespace: NAMESPACE_PLACEHOLDER
  name: SERVICENAME_PLACEHOLDER-bknd-svc-80
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app.kubernetes.io/name: SERVICENAME_PLACEHOLDER-backend-BLUECOLOR_PLACEHOLDER
